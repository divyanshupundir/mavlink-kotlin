package com.urbanmatrix.mavlink.common

import com.urbanmatrix.mavlink.api.AbstractMavDialect
import com.urbanmatrix.mavlink.minimal.MinimalDialect

public object CommonDialect : AbstractMavDialect(
  setOf(
    MinimalDialect
  ), 
  mapOf(
    SysStatus.classMetadata.id to SysStatus.classMetadata,
    SystemTime.classMetadata.id to SystemTime.classMetadata,
    Ping.classMetadata.id to Ping.classMetadata,
    ChangeOperatorControl.classMetadata.id to ChangeOperatorControl.classMetadata,
    ChangeOperatorControlAck.classMetadata.id to ChangeOperatorControlAck.classMetadata,
    AuthKey.classMetadata.id to AuthKey.classMetadata,
    LinkNodeStatus.classMetadata.id to LinkNodeStatus.classMetadata,
    SetMode.classMetadata.id to SetMode.classMetadata,
    ParamRequestRead.classMetadata.id to ParamRequestRead.classMetadata,
    ParamRequestList.classMetadata.id to ParamRequestList.classMetadata,
    ParamValue.classMetadata.id to ParamValue.classMetadata,
    ParamSet.classMetadata.id to ParamSet.classMetadata,
    GpsRawInt.classMetadata.id to GpsRawInt.classMetadata,
    GpsStatus.classMetadata.id to GpsStatus.classMetadata,
    ScaledImu.classMetadata.id to ScaledImu.classMetadata,
    RawImu.classMetadata.id to RawImu.classMetadata,
    RawPressure.classMetadata.id to RawPressure.classMetadata,
    ScaledPressure.classMetadata.id to ScaledPressure.classMetadata,
    Attitude.classMetadata.id to Attitude.classMetadata,
    AttitudeQuaternion.classMetadata.id to AttitudeQuaternion.classMetadata,
    LocalPositionNed.classMetadata.id to LocalPositionNed.classMetadata,
    GlobalPositionInt.classMetadata.id to GlobalPositionInt.classMetadata,
    RcChannelsScaled.classMetadata.id to RcChannelsScaled.classMetadata,
    RcChannelsRaw.classMetadata.id to RcChannelsRaw.classMetadata,
    ServoOutputRaw.classMetadata.id to ServoOutputRaw.classMetadata,
    MissionRequestPartialList.classMetadata.id to MissionRequestPartialList.classMetadata,
    MissionWritePartialList.classMetadata.id to MissionWritePartialList.classMetadata,
    MissionItem.classMetadata.id to MissionItem.classMetadata,
    MissionRequest.classMetadata.id to MissionRequest.classMetadata,
    MissionSetCurrent.classMetadata.id to MissionSetCurrent.classMetadata,
    MissionCurrent.classMetadata.id to MissionCurrent.classMetadata,
    MissionRequestList.classMetadata.id to MissionRequestList.classMetadata,
    MissionCount.classMetadata.id to MissionCount.classMetadata,
    MissionClearAll.classMetadata.id to MissionClearAll.classMetadata,
    MissionItemReached.classMetadata.id to MissionItemReached.classMetadata,
    MissionAck.classMetadata.id to MissionAck.classMetadata,
    SetGpsGlobalOrigin.classMetadata.id to SetGpsGlobalOrigin.classMetadata,
    GpsGlobalOrigin.classMetadata.id to GpsGlobalOrigin.classMetadata,
    ParamMapRc.classMetadata.id to ParamMapRc.classMetadata,
    MissionRequestInt.classMetadata.id to MissionRequestInt.classMetadata,
    SafetySetAllowedArea.classMetadata.id to SafetySetAllowedArea.classMetadata,
    SafetyAllowedArea.classMetadata.id to SafetyAllowedArea.classMetadata,
    AttitudeQuaternionCov.classMetadata.id to AttitudeQuaternionCov.classMetadata,
    NavControllerOutput.classMetadata.id to NavControllerOutput.classMetadata,
    GlobalPositionIntCov.classMetadata.id to GlobalPositionIntCov.classMetadata,
    LocalPositionNedCov.classMetadata.id to LocalPositionNedCov.classMetadata,
    RcChannels.classMetadata.id to RcChannels.classMetadata,
    RequestDataStream.classMetadata.id to RequestDataStream.classMetadata,
    DataStream.classMetadata.id to DataStream.classMetadata,
    ManualControl.classMetadata.id to ManualControl.classMetadata,
    RcChannelsOverride.classMetadata.id to RcChannelsOverride.classMetadata,
    MissionItemInt.classMetadata.id to MissionItemInt.classMetadata,
    VfrHud.classMetadata.id to VfrHud.classMetadata,
    CommandInt.classMetadata.id to CommandInt.classMetadata,
    CommandLong.classMetadata.id to CommandLong.classMetadata,
    CommandAck.classMetadata.id to CommandAck.classMetadata,
    CommandCancel.classMetadata.id to CommandCancel.classMetadata,
    ManualSetpoint.classMetadata.id to ManualSetpoint.classMetadata,
    SetAttitudeTarget.classMetadata.id to SetAttitudeTarget.classMetadata,
    AttitudeTarget.classMetadata.id to AttitudeTarget.classMetadata,
    SetPositionTargetLocalNed.classMetadata.id to SetPositionTargetLocalNed.classMetadata,
    PositionTargetLocalNed.classMetadata.id to PositionTargetLocalNed.classMetadata,
    SetPositionTargetGlobalInt.classMetadata.id to SetPositionTargetGlobalInt.classMetadata,
    PositionTargetGlobalInt.classMetadata.id to PositionTargetGlobalInt.classMetadata,
    LocalPositionNedSystemGlobalOffset.classMetadata.id to
        LocalPositionNedSystemGlobalOffset.classMetadata,
    HilState.classMetadata.id to HilState.classMetadata,
    HilControls.classMetadata.id to HilControls.classMetadata,
    HilRcInputsRaw.classMetadata.id to HilRcInputsRaw.classMetadata,
    HilActuatorControls.classMetadata.id to HilActuatorControls.classMetadata,
    OpticalFlow.classMetadata.id to OpticalFlow.classMetadata,
    GlobalVisionPositionEstimate.classMetadata.id to GlobalVisionPositionEstimate.classMetadata,
    VisionPositionEstimate.classMetadata.id to VisionPositionEstimate.classMetadata,
    VisionSpeedEstimate.classMetadata.id to VisionSpeedEstimate.classMetadata,
    ViconPositionEstimate.classMetadata.id to ViconPositionEstimate.classMetadata,
    HighresImu.classMetadata.id to HighresImu.classMetadata,
    OpticalFlowRad.classMetadata.id to OpticalFlowRad.classMetadata,
    HilSensor.classMetadata.id to HilSensor.classMetadata,
    SimState.classMetadata.id to SimState.classMetadata,
    RadioStatus.classMetadata.id to RadioStatus.classMetadata,
    FileTransferProtocol.classMetadata.id to FileTransferProtocol.classMetadata,
    Timesync.classMetadata.id to Timesync.classMetadata,
    CameraTrigger.classMetadata.id to CameraTrigger.classMetadata,
    HilGps.classMetadata.id to HilGps.classMetadata,
    HilOpticalFlow.classMetadata.id to HilOpticalFlow.classMetadata,
    HilStateQuaternion.classMetadata.id to HilStateQuaternion.classMetadata,
    ScaledImu2.classMetadata.id to ScaledImu2.classMetadata,
    LogRequestList.classMetadata.id to LogRequestList.classMetadata,
    LogEntry.classMetadata.id to LogEntry.classMetadata,
    LogRequestData.classMetadata.id to LogRequestData.classMetadata,
    LogData.classMetadata.id to LogData.classMetadata,
    LogErase.classMetadata.id to LogErase.classMetadata,
    LogRequestEnd.classMetadata.id to LogRequestEnd.classMetadata,
    GpsInjectData.classMetadata.id to GpsInjectData.classMetadata,
    Gps2Raw.classMetadata.id to Gps2Raw.classMetadata,
    PowerStatus.classMetadata.id to PowerStatus.classMetadata,
    SerialControl.classMetadata.id to SerialControl.classMetadata,
    GpsRtk.classMetadata.id to GpsRtk.classMetadata,
    Gps2Rtk.classMetadata.id to Gps2Rtk.classMetadata,
    ScaledImu3.classMetadata.id to ScaledImu3.classMetadata,
    DataTransmissionHandshake.classMetadata.id to DataTransmissionHandshake.classMetadata,
    EncapsulatedData.classMetadata.id to EncapsulatedData.classMetadata,
    DistanceSensor.classMetadata.id to DistanceSensor.classMetadata,
    TerrainRequest.classMetadata.id to TerrainRequest.classMetadata,
    TerrainData.classMetadata.id to TerrainData.classMetadata,
    TerrainCheck.classMetadata.id to TerrainCheck.classMetadata,
    TerrainReport.classMetadata.id to TerrainReport.classMetadata,
    ScaledPressure2.classMetadata.id to ScaledPressure2.classMetadata,
    AttPosMocap.classMetadata.id to AttPosMocap.classMetadata,
    SetActuatorControlTarget.classMetadata.id to SetActuatorControlTarget.classMetadata,
    ActuatorControlTarget.classMetadata.id to ActuatorControlTarget.classMetadata,
    Altitude.classMetadata.id to Altitude.classMetadata,
    ResourceRequest.classMetadata.id to ResourceRequest.classMetadata,
    ScaledPressure3.classMetadata.id to ScaledPressure3.classMetadata,
    FollowTarget.classMetadata.id to FollowTarget.classMetadata,
    ControlSystemState.classMetadata.id to ControlSystemState.classMetadata,
    BatteryStatus.classMetadata.id to BatteryStatus.classMetadata,
    AutopilotVersion.classMetadata.id to AutopilotVersion.classMetadata,
    LandingTarget.classMetadata.id to LandingTarget.classMetadata,
    FenceStatus.classMetadata.id to FenceStatus.classMetadata,
    MagCalReport.classMetadata.id to MagCalReport.classMetadata,
    EfiStatus.classMetadata.id to EfiStatus.classMetadata,
    EstimatorStatus.classMetadata.id to EstimatorStatus.classMetadata,
    WindCov.classMetadata.id to WindCov.classMetadata,
    GpsInput.classMetadata.id to GpsInput.classMetadata,
    GpsRtcmData.classMetadata.id to GpsRtcmData.classMetadata,
    HighLatency.classMetadata.id to HighLatency.classMetadata,
    HighLatency2.classMetadata.id to HighLatency2.classMetadata,
    Vibration.classMetadata.id to Vibration.classMetadata,
    HomePosition.classMetadata.id to HomePosition.classMetadata,
    SetHomePosition.classMetadata.id to SetHomePosition.classMetadata,
    MessageInterval.classMetadata.id to MessageInterval.classMetadata,
    ExtendedSysState.classMetadata.id to ExtendedSysState.classMetadata,
    AdsbVehicle.classMetadata.id to AdsbVehicle.classMetadata,
    Collision.classMetadata.id to Collision.classMetadata,
    V2Extension.classMetadata.id to V2Extension.classMetadata,
    MemoryVect.classMetadata.id to MemoryVect.classMetadata,
    DebugVect.classMetadata.id to DebugVect.classMetadata,
    NamedValueFloat.classMetadata.id to NamedValueFloat.classMetadata,
    NamedValueInt.classMetadata.id to NamedValueInt.classMetadata,
    Statustext.classMetadata.id to Statustext.classMetadata,
    Debug.classMetadata.id to Debug.classMetadata,
    SetupSigning.classMetadata.id to SetupSigning.classMetadata,
    ButtonChange.classMetadata.id to ButtonChange.classMetadata,
    PlayTune.classMetadata.id to PlayTune.classMetadata,
    CameraInformation.classMetadata.id to CameraInformation.classMetadata,
    CameraSettings.classMetadata.id to CameraSettings.classMetadata,
    StorageInformation.classMetadata.id to StorageInformation.classMetadata,
    CameraCaptureStatus.classMetadata.id to CameraCaptureStatus.classMetadata,
    CameraImageCaptured.classMetadata.id to CameraImageCaptured.classMetadata,
    FlightInformation.classMetadata.id to FlightInformation.classMetadata,
    MountOrientation.classMetadata.id to MountOrientation.classMetadata,
    LoggingData.classMetadata.id to LoggingData.classMetadata,
    LoggingDataAcked.classMetadata.id to LoggingDataAcked.classMetadata,
    LoggingAck.classMetadata.id to LoggingAck.classMetadata,
    VideoStreamInformation.classMetadata.id to VideoStreamInformation.classMetadata,
    VideoStreamStatus.classMetadata.id to VideoStreamStatus.classMetadata,
    CameraFovStatus.classMetadata.id to CameraFovStatus.classMetadata,
    CameraTrackingImageStatus.classMetadata.id to CameraTrackingImageStatus.classMetadata,
    CameraTrackingGeoStatus.classMetadata.id to CameraTrackingGeoStatus.classMetadata,
    GimbalManagerInformation.classMetadata.id to GimbalManagerInformation.classMetadata,
    GimbalManagerStatus.classMetadata.id to GimbalManagerStatus.classMetadata,
    GimbalManagerSetAttitude.classMetadata.id to GimbalManagerSetAttitude.classMetadata,
    GimbalDeviceInformation.classMetadata.id to GimbalDeviceInformation.classMetadata,
    GimbalDeviceSetAttitude.classMetadata.id to GimbalDeviceSetAttitude.classMetadata,
    GimbalDeviceAttitudeStatus.classMetadata.id to GimbalDeviceAttitudeStatus.classMetadata,
    AutopilotStateForGimbalDevice.classMetadata.id to AutopilotStateForGimbalDevice.classMetadata,
    GimbalManagerSetPitchyaw.classMetadata.id to GimbalManagerSetPitchyaw.classMetadata,
    GimbalManagerSetManualControl.classMetadata.id to GimbalManagerSetManualControl.classMetadata,
    EscInfo.classMetadata.id to EscInfo.classMetadata,
    EscStatus.classMetadata.id to EscStatus.classMetadata,
    WifiConfigAp.classMetadata.id to WifiConfigAp.classMetadata,
    AisVessel.classMetadata.id to AisVessel.classMetadata,
    UavcanNodeStatus.classMetadata.id to UavcanNodeStatus.classMetadata,
    UavcanNodeInfo.classMetadata.id to UavcanNodeInfo.classMetadata,
    ParamExtRequestRead.classMetadata.id to ParamExtRequestRead.classMetadata,
    ParamExtRequestList.classMetadata.id to ParamExtRequestList.classMetadata,
    ParamExtValue.classMetadata.id to ParamExtValue.classMetadata,
    ParamExtSet.classMetadata.id to ParamExtSet.classMetadata,
    ParamExtAck.classMetadata.id to ParamExtAck.classMetadata,
    ObstacleDistance.classMetadata.id to ObstacleDistance.classMetadata,
    Odometry.classMetadata.id to Odometry.classMetadata,
    TrajectoryRepresentationWaypoints.classMetadata.id to
        TrajectoryRepresentationWaypoints.classMetadata,
    TrajectoryRepresentationBezier.classMetadata.id to TrajectoryRepresentationBezier.classMetadata,
    CellularStatus.classMetadata.id to CellularStatus.classMetadata,
    IsbdLinkStatus.classMetadata.id to IsbdLinkStatus.classMetadata,
    CellularConfig.classMetadata.id to CellularConfig.classMetadata,
    RawRpm.classMetadata.id to RawRpm.classMetadata,
    UtmGlobalPosition.classMetadata.id to UtmGlobalPosition.classMetadata,
    DebugFloatArray.classMetadata.id to DebugFloatArray.classMetadata,
    OrbitExecutionStatus.classMetadata.id to OrbitExecutionStatus.classMetadata,
    SmartBatteryInfo.classMetadata.id to SmartBatteryInfo.classMetadata,
    GeneratorStatus.classMetadata.id to GeneratorStatus.classMetadata,
    ActuatorOutputStatus.classMetadata.id to ActuatorOutputStatus.classMetadata,
    TimeEstimateToTarget.classMetadata.id to TimeEstimateToTarget.classMetadata,
    Tunnel.classMetadata.id to Tunnel.classMetadata,
    CanFrame.classMetadata.id to CanFrame.classMetadata,
    OnboardComputerStatus.classMetadata.id to OnboardComputerStatus.classMetadata,
    ComponentInformation.classMetadata.id to ComponentInformation.classMetadata,
    ComponentMetadata.classMetadata.id to ComponentMetadata.classMetadata,
    PlayTuneV2.classMetadata.id to PlayTuneV2.classMetadata,
    SupportedTunes.classMetadata.id to SupportedTunes.classMetadata,
    Event.classMetadata.id to Event.classMetadata,
    CurrentEventSequence.classMetadata.id to CurrentEventSequence.classMetadata,
    RequestEvent.classMetadata.id to RequestEvent.classMetadata,
    ResponseEventError.classMetadata.id to ResponseEventError.classMetadata,
    CanfdFrame.classMetadata.id to CanfdFrame.classMetadata,
    CanFilterModify.classMetadata.id to CanFilterModify.classMetadata,
    WheelDistance.classMetadata.id to WheelDistance.classMetadata,
    WinchStatus.classMetadata.id to WinchStatus.classMetadata,
    OpenDroneIdBasicId.classMetadata.id to OpenDroneIdBasicId.classMetadata,
    OpenDroneIdLocation.classMetadata.id to OpenDroneIdLocation.classMetadata,
    OpenDroneIdAuthentication.classMetadata.id to OpenDroneIdAuthentication.classMetadata,
    OpenDroneIdSelfId.classMetadata.id to OpenDroneIdSelfId.classMetadata,
    OpenDroneIdSystem.classMetadata.id to OpenDroneIdSystem.classMetadata,
    OpenDroneIdOperatorId.classMetadata.id to OpenDroneIdOperatorId.classMetadata,
    OpenDroneIdMessagePack.classMetadata.id to OpenDroneIdMessagePack.classMetadata,
    HygrometerSensor.classMetadata.id to HygrometerSensor.classMetadata,
  )
)
